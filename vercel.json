{
    "version": 2,
    " builds": [
    {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
            "distDir": "dist/tailwind-learn/browser"
        }
    }
],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}