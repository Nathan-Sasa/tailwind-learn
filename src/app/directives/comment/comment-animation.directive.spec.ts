import { CommentAnimationDirective } from './comment-animation.directive';

describe('CommentAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
