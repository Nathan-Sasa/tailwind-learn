import { ProjetBgDirective } from './projet-bg.directive';

describe('ProjetBgDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjetBgDirective();
    expect(directive).toBeTruthy();
  });
});
