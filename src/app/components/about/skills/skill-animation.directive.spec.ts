import { SkillAnimationDirective } from './skill-animation.directive';

describe('SkillAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new SkillAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
